# Lint Python files for syntax errors and undefined names.
# This repo contains skill scripts; they are not a Python package and have no
# test suite that can run from the repo root, so pytest is intentionally omitted.

name: Python lint

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install flake8
      run: pip install flake8
    - name: Lint — syntax errors and undefined names
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
